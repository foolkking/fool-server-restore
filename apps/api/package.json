{
  "name": "@fool/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node --watch dist/server.js",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/engine/tests/*.test.js"
  },
  "dependencies": {
    "@fool/collectors": "0.1.0",
    "@fool/core": "0.1.0",
    "@fool/restorers": "0.1.0",
    "fastify": "^4.28.1",
    "ssh2": "^1.17.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/ssh2": "^1.15.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
