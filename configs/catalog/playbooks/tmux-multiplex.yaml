# Playbook: tmux 终端复用器
#
# 包名审查矩阵 (§7.5.2)：
#   tmux — Debian ✅；RHEL 默认仓库 ✅（也在 EPEL 里）
name: Install tmux with sane defaults
hosts: all

tasks:
  - name: Install tmux
    module: package
    args:
      name: tmux
      state: present

  - name: Configure tmux defaults
    module: copy
    args:
      dest: ~/.tmux.conf
      content: |
        # Mouse support
        set -g mouse on
        # 256 colors
        set -g default-terminal "screen-256color"
        # History
        set -g history-limit 10000
        # Better split shortcuts
        bind | split-window -h
        bind - split-window -v

  - name: Verify tmux
    module: shell
    args:
      cmd: "tmux -V"
