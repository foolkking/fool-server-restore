{
  "listen_address": {
    "type": "choice",
    "label": "监听地址",
    "labelEn": "Listener address",
    "default": "127.0.0.1",
    "options": [
      { "value": "127.0.0.1", "label": "127.0.0.1（仅本机，推荐）" },
      { "value": "0.0.0.0", "label": "0.0.0.0（所有网卡，**必须先配 TLS**）" }
    ],
    "help": "Vault 默认本 Playbook 配置 tls_disable=1（明文）。改成 0.0.0.0 暴露到公网前必须先配 TLS 证书，否则所有 secret 都明文传输。"
  },
  "api_port": {
    "type": "port",
    "label": "API 端口",
    "labelEn": "API port",
    "default": 8200,
    "help": "Vault API + UI 端口。默认 8200。"
  },
  "storage_path": {
    "type": "string",
    "label": "数据存储路径",
    "labelEn": "Storage path",
    "default": "/var/lib/vault/data",
    "validate": "^/[a-zA-Z0-9/_.-]+$",
    "help": "Vault 数据存储位置。生产环境建议放在专用磁盘 / 备份频繁的目录。"
  }
}
