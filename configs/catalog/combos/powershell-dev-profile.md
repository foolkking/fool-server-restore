# PowerShell 开发偏好组合

## 组合内容

- 常用 alias
- PowerShell Profile 片段
- 终端提示符偏好
- 常用模块列表

## 用户说明

建议只上传 alias、函数和模块名，不上传包含 token 的环境变量。

```powershell
Get-Alias
$PROFILE
```

## 隐私说明

Profile 中的密钥、token、私有路径应先脱敏。
